# Deque

My implementation of the `std::deque` container.

**Key features:**
* Fast O(1) push and pop at both ends.
* Custom `iterator` and `const_iterator`.
* Strict exception safety (no memory leaks if an exception is thrown).
